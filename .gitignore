# Environment variables / secrets
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Logs
logs/
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor/IDE
.vscode/
.idea/
*.swp

# Package manager lock inconsistencies (keep ONE of these, not both)
# If you use npm, keep package-lock.json and ignore these instead:
# yarn.lock
# pnpm-lock.yaml

# Build output (if you ever add a bundler later)
dist/
build/

# Uploaded files (if project/portrait images get saved locally instead of a cloud bucket)
uploads/
